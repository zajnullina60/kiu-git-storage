небходимо установить:
import json
from flask import Flask, jsonify, request
использовалась программа Postman( - платформа API для разработчиков, позволяющая проектировать, создавать, тестировать и повторять свои API)
